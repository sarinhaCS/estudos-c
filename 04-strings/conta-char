#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
  int cont = 0;
  printf("Digite uma frase: ");
  while(getche() != '\r')
    cont++;
  printf("\nO numero de caracteres e %d \n", cont);
  
  return 0;
}
